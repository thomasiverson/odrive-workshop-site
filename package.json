{
  "name": "odrive-workshop-site",
  "type": "module",
  "version": "1.0.0",
  "description": "Astro site for ODrive Copilot workshop lessons",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/sync-content.mjs --repo thomasiverson/ODrive-Custom && astro build",
    "build:local": "node scripts/sync-content.mjs --source ../ODrive-Custom && astro build",
    "sync": "node scripts/sync-content.mjs --source ../ODrive-Custom",
    "sync:remote": "node scripts/sync-content.mjs --repo thomasiverson/ODrive-Custom",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^5.7.0"
  },
  "devDependencies": {}
}
